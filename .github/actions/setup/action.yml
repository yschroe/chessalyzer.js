name: Setup
description: Bun runtime and dependency installation

runs:
  using: composite
  steps:
    - uses: oven-sh/setup-bun@v2
      with:
        bun-version: latest

    - name: Install dependencies
      run: bun ci

    - name: Build
      run: bun run build
