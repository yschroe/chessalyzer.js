// Reexport from root so we can use it as a build entry.
// File needs to live next to bundled game-processor.js (see `WORKER_PATH`).
// oxlint-disable-next-line import/no-unassigned-import
import '#core/chess-worker';
